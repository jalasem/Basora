/* Tiny dependency-free ZIP writer: store method only, UTF-8 filenames. */
function basoraCrc32(bytes) { let c = -1; for (const b of bytes) { c ^= b; for (let k = 0; k < 8; k++) c = (c >>> 1) ^ (-(c & 1) & 0xedb88320); } return (c ^ -1) >>> 0; }
function basoraZip(entries) { const enc = new TextEncoder(); const chunks = []; const central = []; let offset = 0; const u16 = (n) => { const a = new Uint8Array(2); new DataView(a.buffer).setUint16(0, n, true); return a; }; const u32 = (n) => { const a = new Uint8Array(4); new DataView(a.buffer).setUint32(0, n >>> 0, true); return a; }; const join = (parts) => { const out = new Uint8Array(parts.reduce((n, p) => n + p.length, 0)); let i = 0; for (const p of parts) { out.set(p, i); i += p.length; } return out; }; for (const entry of entries) { const name = enc.encode(entry.name); const data = entry.data instanceof Uint8Array ? entry.data : enc.encode(entry.data); const crc = basoraCrc32(data); const flags = 0x0800; const local = join([new Uint8Array([80,75,3,4]), u16(20), u16(flags), u16(0), u16(0), u16(0), u32(crc), u32(data.length), u32(data.length), u16(name.length), u16(0), name, data]); chunks.push(local); central.push(join([new Uint8Array([80,75,1,2]), u16(20), u16(20), u16(flags), u16(0), u16(0), u16(0), u32(crc), u32(data.length), u32(data.length), u16(name.length), u16(0), u16(0), u16(0), u16(0), u32(0), u32(offset), name])); offset += local.length; } const directory = join(central); const body = join(chunks); const end = join([new Uint8Array([80,75,5,6]), u16(0), u16(0), u16(entries.length), u16(entries.length), u32(directory.length), u32(body.length), u16(0)]); return join([body, directory, end]); }
